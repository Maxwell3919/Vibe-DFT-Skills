# Changelog

All notable repository-governance, contract, lifecycle, routing, scientific-evidence, and release changes should be recorded here.

The format follows Keep a Changelog principles, with additional sections for lifecycle and evidence-boundary changes. Versions follow semantic versioning where a formal release exists.

## Unreleased

### Added

- A local-only official-manual Markdown cache with per-Skill routing, pinned
  provider receipts, character-preservation checks, internal-link validation,
  and a stable `OM-GAP-*` ledger for unresolved upstream evidence.
- A deterministic calculation workspace and revisioned taskbook with `off`,
  `silent-update`, and `milestone-review` modes.
- CIF structure-intelligence screening for formula/Z and cell quality, nearest-shell local geometry, multi-scale periodic-graph dimensionality, symmetry-only property hypotheses, and explicitly unranked source/primitive/conventional optimization starting points.
- Central registries and per-Skill routes for first-party websites, manuals,
  source repositories, release channels, four active calculation codes,
  nineteen planned software identities, and direct standards/infrastructure
  dependencies.
- Content-first planning, audit, troubleshooting, restart, output-acceptance, convergence, and scientific-boundary playbooks across all 19 development Skills, grounded in version-matched official manuals or upstream source where public evidence permits.
- Architecture baseline records for source commit `33dc893ad7dee6cdab8d645f7e25ff44f345c899`.
- Repository contribution and review policy.
- Security reporting and containment policy.
- Release policy.
- Lifecycle promotion policy.
- CODEOWNERS coverage for governance, contracts, registries, tools, and Skill source.
- Exact official-document pack discovery, a one-way source-Skill expectation
  ledger, and Git-baseline migration checks.
- A worktree-aware legacy official-document storage gate with exact artifact
  and local-control identities, central authority all-of evaluation, and
  delete-only post-bootstrap migration.
- Deterministic official-document registration packs for all 26 source-backed
  Skills, backed by 57 exact authority-to-consumer bindings, bundle
  expectations, version-scoped and hash-bound seed/source/scope catalogs, and
  a locked repository builder. The 57 corpus manifests record 3,421 discovered
  source identities as 462 inclusions plus 2,959 reviewed exclusions.
- A four-layer official-document assurance model that reports registration,
  inventory, content materialization, and semantic slicing independently.

### Changed

- Consolidated every extant GitHub branch tip into `main` ancestry. The two
  MacBook backup tips were history-only equivalents of trees already merged
  through `24dd8a9`/`e1c97e6` and `66f5aff`; their ancestry was retained without
  reintroducing obsolete generated content.
- Rewrote the README as a concise scientific workflow narrative centered on
  questions, provenance, convergence, reviewable artifacts, and claim-evidence
  boundaries.
- Moved full official-manual bodies out of Git while retaining receipts,
  hashes, source identities, gap records, deterministic rebuild tooling, and
  local readable caches.
- Reconciled the planned environment authority with the content review: the
  internal `ovito-basic` profile now means the standalone `ovito` Python
  module, while LASP records only its publicly visible edition and compiler/MPI
  baseline without treating them as native execution evidence.
- Made CP2K official-source and claim-package tests hermetic by injecting a
  synthetic provider-cache fixture. A full external mirror is still validated
  when installed, as is the pinned external html2md converter; their absence no
  longer makes a clean GitHub runner depend on a developer workstation cache
  or tool installation.
- Updated repository-facing documentation for public GitHub visibility without changing Skill lifecycles, scientific evidence gates, third-party license boundaries, or the absence of a top-level repository license.
- Expanded the repository extension checklist so every new or enlarged
  official-document pack must close its central authority, consumer binding,
  expectation, seed/source/scope catalogs, deterministic generated pack, and
  `tools/build_official_document_packs.py --all --check`.
- Closed red-team false-positive paths by running the canonical builder check
  in CI, content-binding any claimed exact license-terms bytes, projecting
  blockers only to applicable dimensions, enforcing exact corpus-to-slice and
  bidirectional loss-ledger closure, rejecting declarative inventory
  self-certification, binding rolling-source aggregates, independently
  auditing portable active-only archives, and deriving dashboard rows from
  cross-validated registries.

### Lifecycle

- No Skill lifecycle state changed.
- No software identity lifecycle state changed.
- The repository remains at 7 active Skills, 19 development Skills, 0 planned Skill identities, 4 active calculation-code integrations, and 19 planned software identities for the recorded baseline.

### Evidence boundary

- The canonical Git tree now contains zero legacy official-manual body
  artifacts. Local materialization passed for 14,635 Markdown or metadata-route
  documents, but the ordinary bundle audit remains
  `0 complete / 26 partial / 0 missing / 0 invalid`.
- The explicit upstream ledger records two unavailable Multiwfn PDF bodies, an
  unavailable MACE search index, twelve LASP/LOBSTER literature-only publisher
  records, two absent SIESTA images, ten missing or non-public VASP Wiki titles,
  and four intentional nonmanual source-tree records.
- A taskbook revision records workflow state and artifact identity; it does not
  turn directory placement, execution completion, or a rendered figure into
  scientific acceptance.
- CIF quality, topology, property screens, standardized cells, and optimization candidates do not establish energetic, dynamic, or thermodynamic stability, nonzero response, synthesizability, or complete official-document capture.
- Content depth now supports preliminary planning and audit use; it does not establish installed-software availability, native execution, end-to-end numerical validity, or scientific acceptance.
- The baseline records architecture and declared lifecycle state only.
- No native third-party software execution, numerical convergence, physical validity, or scientific acceptance claim is added.
- Registration packs are now generated for 26/26 source-backed Skills, but the
  ordinary bundle audit remains `0 complete / 26 partial`.
- All 1,586 canonical-pack slices remain `metadata-only` (421 `whole-source`,
  1,159 `source-symbol`, and 6 `json-pointer`), with zero materialized official
  document bodies in the canonical pack domain.
- Pack presence proves neither complete official-content inventory nor content
  materialization or semantic slicing. Complete, legally reviewable production
  coverage therefore remains a release blocker; contract tests, synthetic
  fixtures, metadata-only identities, and legacy mirrors do not satisfy that
  gate.
- Before the metadata-only migration, the candidate contained 2,075 centrally
  forbidden legacy official-document artifacts outside the canonical pack
  domain. The migration removed those bodies from Git; the historical count is
  retained here as the storage transition baseline.
- The stable dashboard remains fail closed: corpus is `11 partial / 15
  blocked`, slice `10 partial / 16 blocked`, scope `5 partial / 21 blocked`,
  license `26 partial / 0 blocked`, storage `22 partial / 4 blocked`, and
  freshness `26 unknown`. Registration is `26 complete`; inventory is `11
  partial / 15 blocked`; content materialization is `10 missing / 16 blocked`;
  semantic slicing is `1 partial / 9 missing / 16 blocked`; assurance overall
  is `10 missing / 16 blocked`; final overall is `1 missing / 25 blocked`.
- The 57 provider records remain `32 partial / 25 blocked` for corpora, `29
  partial / 28 blocked` for slice manifests, and `57 partial / 0 blocked` for
  license reviews. Only the canonical CP2K manual corpus passes
  `upstream_universe_complete`.
- Remaining evidence gaps include subject-to-slice mapper attestation,
  independent inventory wiring for 15 blocked Skill rows, materialized
  adapters and authoritative license attestation, and 52/52 live drift checks
  reported as `unavailable`, not unchanged.

## v0.1.0-architecture-baseline - proposed

This identifier is reserved for the reviewed architecture baseline. The tag is not considered created until an explicit Git operation records it after validation and review.
